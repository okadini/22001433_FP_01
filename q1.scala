import scala.math._

object q1 {
    def main(args:Array[String]) = {

        def diskArea(r:Double) = {
            Pi * r * r
        }

        println("Area of the disk = " + diskArea(5))
    }
}